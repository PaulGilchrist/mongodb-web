// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
export const environment = {
  // Do not use Angular environment files as they are not compatible with containers.  Use ./assets/env-default.txt and ./assets/env-config.js instead
  production: false
};

